import ProjectCarousel from './ProjectCarousel';

const Projects = () => {
  return (
    <section id="projects">
      <div>
        <h1>Projects 💻</h1>
      </div>
      <ProjectCarousel />
    </section>
  );
};

export default Projects;
